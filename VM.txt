var x, z, w, a, i: int;
var xx: float;
var y: string;


int func1(int x, int b, string j){
	var pedro, juan, jose: int;
	return 1;
	
}

float func2(int l, int u){
	return 1.5;
}

main{
	x = 5;
	z = 50/5;
	w = 100 * 10;
	a = 2 - 1;
	y = "Hello World";
	x = x + x + x + x + x ;
	i = 1;

	while (i < 6) {
		print("Ciclo completo numero:");
		print (i);
		i = i + 1;
	}

	xx = func1 (x + 1, a * 5 / 6 + (5 + 4), y) * func2 (x, a);

}